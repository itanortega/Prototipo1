<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="bacgroundActivity">
        <item name="android:background">@drawable/fnd</item>
    </style>

    <style name="editTextS">
        <item name="android:textColor">@color/colorTextS</item>
        <item name="android:background">@color/colorBTextS</item>
        <item name="android:paddingLeft">16dp</item>
    </style>

    <style name="textViewS">
        <item name="android:textColor">@color/colorTextS</item>
    </style>
</resources>
